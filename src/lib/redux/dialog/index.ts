import dialogReducer from './dialog.slice';

export { dialogReducer };
export { selectDialog } from './dialog.selectors';
export { setDialog } from './dialog.slice';
export type { DialogProps, DialogState, DialogSize } from './dialog.types';
